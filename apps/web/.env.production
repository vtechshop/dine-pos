# Vite production overrides — applied automatically on `vite build`.
# These values are baked into the JS bundle at build time.
# Override in CI/CD env if deploying to a different domain.
VITE_API_URL=https://api.dinepos.com/api
VITE_SOCKET_URL=https://api.dinepos.com
